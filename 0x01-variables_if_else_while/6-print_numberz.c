#include <stdio.h>
/**
 * main-prints 0 to 9
 *
 * retunr always 0(success)
 */
int main(void)
{
        int a;

        for(a=21;a<31;a++)
        {
                putchar(a);
		putchar('\n');
		
        }
        
        return (0);
}
